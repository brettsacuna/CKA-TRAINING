apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "gratitud.fullname" . }}
  labels:
    {{- include "gratitud.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.config }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
