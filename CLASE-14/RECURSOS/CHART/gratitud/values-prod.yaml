# Sobrescrituras para el entorno de produccion: helm upgrade ... -f values-prod.yaml
replicaCount: 4

image:
  tag: "1.27-alpine"

ingress:
  enabled: true
  host: gratitud.produccion.example.com

resources:
  requests: {cpu: 100m, memory: 64Mi}
  limits: {cpu: 500m, memory: 256Mi}
